# Disable ChromaDB telemetry (required — prevents phoning home)
ANONYMIZED_TELEMETRY=False

# GitHub Models provider (for --provider github)
# Create a PAT with 'models' scope at https://github.com/settings/tokens
# GITHUB_TOKEN=ghp_...
